check process redis-<%= @profile %>
with pidfile <%= @pidfile %>
  start program = "/usr/sbin/redis-server <%= @configfile %>"
  stop program = "/usr/sbin/redis-cli -p <%= @port %> shutdown"
  group redis-util
